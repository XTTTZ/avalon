# 阿瓦隆手机版 Web App 实施计划

## 目标与交付

实现供朋友线下面对面使用的 React + TypeScript Web App。公众号菜单打开 HTTPS 网页，生产后端使用 Tencent CloudBase 云函数和文档数据库，无需自建服务器。本地使用同一套游戏引擎和文件持久化 API，开发不依赖微信或云账号。

## 实施顺序

- [x] 1. 建立共享类型、标准规则表、运行脚手架与环境配置。
- [x] 2. 实现服务端状态机：大厅、发身份、确认身份、组队、投票、任务、湖中仙女、刺杀、结算。
- [x] 3. 实现可信身份、仅本人秘密投影、匿名任务票、私人笔记、版本控制、幂等命令与事务存储。
- [x] 4. 实现手机优先界面：建房/加入/二维码、房间规则、玩家列表、隐私身份卡、行动面板、记录与笔记。
- [x] 5. 实现刷新/重开恢复、前后台切换、网络恢复、低频自适应 polling；不轮询写入在线心跳。
- [x] 6. 完成规则、权限、并发/重放、恢复和完整游戏流程测试，构建并在手机尺寸浏览器验证。
- [x] 7. 提供简洁 README、CloudBase 部署产物、数据库规则与微信公众号接入说明。

## 确定的规则边界

- 标准 5–10 人：阵营人数分别为 3/2、4/2、4/3、5/3、6/3、6/4；使用官方五轮任务表，连续五次拒绝组队邪恶胜；7 人及以上第四轮两张失败票才失败。
- 11–12 人明确标记为自定义扩展。自定义允许修改阵营、角色、3–9 轮任务、人数、失败阈值、获胜任务数和连续拒绝上限，服务端校验不存在不可能完成或无确定胜负的配置。
- 湖中仙女在第 2、3、4 次任务结束且游戏未结束时行动；持有人查看一名从未持有湖中仙女的玩家当前阵营，再把标记交给对方。
- 兰斯洛特使用明确标注的本应用扩展：成对加入、初始一善一恶，可选从第三轮开始在任务轮开始时公开抽取阵营变化牌；同步交换当前阵营。详细知情边界与牌组写入规则帮助及测试，不冒充标准规则。
- 活跃游戏不能换人、踢人或代投；掉线等待本人恢复。房主可显式结束整局；重新开局须重新发身份。

## 架构与安全

- `shared/`：前后端契约、角色文案、无秘密的规则配置。
- `server/`：纯游戏状态机、严格命令校验、按玩家投影、存储接口、身份与 HTTP API。
- `src/`：React 手机界面，业务状态以服务端为准；浏览器仅保存会话凭据、房间位置和本人 UI 草稿。
- `cloudfunctions/avalon/`：构建生成的 CloudBase Node 云函数；所有集合禁止客户端直读直写。
- 房间秘密仅存在服务端；响应采用显式字段白名单，不依赖 CSS 隐藏权限。任务票仅返回收票数量和最终聚合值，结束后也不公开个人任务票。
- 私人笔记按可信会话所有者独立存储，房主没有读取他人笔记的接口。
- 云端房间写入使用事务；客户端携带版本与幂等请求 ID。投票允许同阶段并发提交，其他过期命令拒绝，重试不会重复推进。
- 生产 OpenID 只由服务端用微信授权 code 换取；不信任客户端提交的 OpenID/playerId。OAuth state 校验，AppSecret 仅放云函数环境变量。
- 身份卡默认隐藏；长按松开、离开窗口、切后台、超时和路由/阶段变化自动遮盖；秘密不写浏览器持久缓存。

## 成本与运维

- 前台自适应轮询，后台/离线暂停，恢复后立即拉取；版本未变只返回轻量结果。
- 读取不写入数据库，不建立持续 WebSocket。设置房间过期时间及定期批量清理，限制房间大小、文本长度与操作频率。
- 免费额度和套餐资格以部署时 CloudBase 控制台为准，提供请求量估算和预算提醒配置，不承诺永久免费。

## 验收

本地一条命令启动；5–12 人规则验证；善恶获胜与刺杀分支；湖中仙女与兰斯洛特分支；非成员/非房主/非队长越权拒绝；秘密信息不泄漏；重复和并发投票安全；刷新恢复；移动端构建与浏览器端到端测试。实际云端部署、真实微信 OAuth 与真机微信验收需要用户自己的 CloudBase 环境、公众号资质和域名，代码与步骤应全部准备好。

## 完成记录（2026-09-17）

- 已交付全部应用代码、React 手机界面、本地持久化服务、CloudBase 部署包、数据库配置与 README。
- 58 项自动测试通过，覆盖标准/扩展规则、角色知情边界、身份认证、OAuth state、权限、事务、幂等、笔记隔离与恢复竞态。
- 5 项浏览器/HTTP 端到端场景通过：身份遮盖与重新打开恢复、完整五人局、断网恢复、建房/扩展配置/二维码、丢失响应后按原 ID 重试。
- TypeScript 检查和生产构建通过；前端未包含服务端身份表、任务票映射或云端密钥。根项目与独立云函数包的 npm audit 均为 0 项漏洞。
- 已检查桌面和手机截图；320、390、768、1280 像素宽度无横向溢出，浏览器无未处理页面错误。
- 云函数构建可加载，缺少生产配置时拒绝服务；独立依赖安装、CloudBase SDK 事务接口与数据库适配器合同验证通过。
- 尚未进行真实云端发布或微信真机授权：需要使用者的 CloudBase 环境、公众号 AppID/AppSecret、授权域名；部署步骤与验收清单已提供，不需要再实现业务代码。

## 手机界面简化（2026-09-17）

- [x] 去掉文艺提示、英文装饰和私人笔记等功能的重复说明，保留必要的游戏规则与操作反馈。
- [x] 公共正文和玩家名字改为 16px，主要按钮 17px、点击区域至少 44px；使用系统无衬线字体，身份详情保持较小字号。
- [x] 当前行动置于圆桌页顶部，任务票直接在圆桌提交，不再依赖身份显示或 5 秒计时。双方按钮相同，好人失败票仍由服务端拒绝，个人任务票不公开。
- [x] 按好人/坏人列出公开角色配置与数量，不公开玩家身份对应关系。
- [x] 精简笔记布局，处理长名字导致横向玩家栏撑高的问题。
- [x] 6 项端到端测试通过；新增覆盖投票不受 5 秒限制、好人失败票拒绝、匿名汇总及公开角色分组。TypeScript 检查与生产构建通过。
- [x] 检查首页、大厅、身份、笔记、规则弹窗和任务投票截图；320、390、768、1280 像素无页面横向溢出，包含 12 人和 9 轮任务的窄屏检查。

## 四位房间号与带队顺序（2026-09-17）

- [x] 新建房间使用随机四位数字；以房间号作为文档主键，在同一事务内检查占用并创建，碰撞时换号重试，绝不覆盖已有房间。兼容已存在的六位房间。
- [x] 首版完成大厅手动上下调整、反向及随机排序，首位随机后循环；局中权限由下节扩展。
- [x] 圆桌展示公开带队队列，标明当前及下一位队长；排序保留玩家 ID、会话及私人笔记。
- [x] 65 项自动测试、7 项端到端测试、TypeScript 检查和生产构建通过；覆盖房间号碰撞/并发与过期占用、排序权限/循环/持久化/幂等、四位号码加入及手机排序。
- [x] 12 人及长名字场景在 320、390、768、1280 像素下无横向溢出；README、架构说明及本地服务已更新。

## 局中改序与手动指定（2026-09-17）

- [x] 扩展房主改序权限至局中，保留当前队长、身份、已提交的票和私人笔记。
- [x] 增加按序循环 / 房主指定两种方式；手动模式每次等待房主选人，可连续指定同一玩家。
- [x] 确认身份/选人时可立即指定当前队长；投票中可预约或取消下一任，轮换时生效，不作废已有投票。
- [x] 兼容旧房间；保留新四位房间号、碰撞重试、刷新恢复与幂等机制。
- [x] 68 项自动测试、8 项端到端测试、TypeScript 检查、生产构建和格式检查通过；覆盖局中改序、手动指定、已有投票保留、下一任预约及刷新恢复。
- [x] 检查 12 人、长名字的排序与指定队长弹窗；320、390、768、1280 像素无页面横向溢出，预约后的队列与手动模式刷新恢复正确，浏览器无未处理页面错误。

## 首次上线与 CI/CD 指南（2026-09-17）

- [x] 核对 CloudBase CLI、部署凭据和 GitHub 免费私有仓库的配置限制；固定 CLI 3.8.2，改用仓库级 Secrets / Variables。
- [x] 添加 CI 与标签/手动发布工作流；本地 guest 测试不访问生产，发布前重新验证，生产发布串行执行。
- [x] 日常发布使用 code update，保留云函数环境变量与触发器；网站发布备份并校验。
- [x] 编写逐步操作文档，包含首次 GitHub 上传、guest 上线、微信接入、部署子用户/权限参考、后台配置、手动/标签发布、回滚和排错；区分公开信息、登录授权与秘密存放位置。
- [x] 工作流 YAML、触发器结构和 shell 语法检查通过；12 个发布配置校验场景、CLI 3.8.2 命令/源码核对、TypeScript/生产构建及格式检查通过。真实 GitHub Actions、目标 CAM 权限、CloudBase 首次发布和微信授权仍需用户登录及目标环境配置，未声称已上线。

## 用户环境首次部署（2026-09-17，更新上述未上线状态）

- [x] 验证 GitHub 空私有仓库访问权限及 CloudBase 管理员环境 API Key；确认新加坡国内站体验版使用 PostgreSQL，没有文档数据库。
- [x] 添加 PostgreSQL 服务端存储适配、UUID 修订号、按键锁定的原子提交和冲突重试；客户端无表/RPC 权限。
- [x] 首次云端在线依赖构建失败；改为本地/CI 安装生产依赖并上传，关闭云端安装依赖，成功部署 Event 云函数及 HTTP 路由。
- [x] 部署 PostgreSQL 表与 RPC、身份密钥、guest 模式、精确 CORS origin、每小时清理触发器及默认 HTTPS 网页。
- [x] 真实云端验证缺失键竞争、并发更新不丢失、失败回调不写入及匿名拒绝；通过完整五人云端游戏、私人笔记隔离、设备恢复、并发投票、刺杀和再来一局，清理测试记录。
- [x] 本地 73 项测试、8 项端到端测试、类型检查、生产构建、格式及工作流 YAML 检查通过；移除旧重复测试工作流，CI 支持环境 API Key。
- [x] 添加针对用户实际环境的操作单，包含五个公开配置值、单个 GitHub Secret、发布方式和安全轮换顺序；密钥不进入源代码。
- [ ] 在 GitHub 填写 Secret / Variables，并完成自动发布验收。
- [ ] 用户完成真实微信浏览器五手机验收；公众号/OpenID 等账号和授权域名具备后再接入。
